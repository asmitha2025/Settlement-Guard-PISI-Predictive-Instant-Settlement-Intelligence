# PISI Core Pipeline is Python Standard Library Only (stdlib-only).
# No external packages are required to run `demo_scenario.py` or `batch_eval.py`.

# Optional packages for extended UI / API layers (FastAPI & Streamlit dashboard):
# fastapi>=0.104.1
# uvicorn>=0.24.0
# streamlit>=1.28.0
# pydantic>=2.5.0
# pyyaml>=6.0.1
